function createTickets(name, department, requestor, description, hour, minute, day, month, year){
    
    alert("this is when tickets would be created")
    /*$.getJSON("/assets/json/tickets.json",
        function (data) {
            var ticket = {name: name, department: department, requestor: requestor, description: description, date: day + "/" + month + "/" + year, time: hour + ":" + minute}

            data.push(ticket);
            alert("Works")
            $.post();
        }
    )*/
}
